{
  "_comment": "Placeholder — scripts/setup.sh rewrites this with absolute .venv paths for your machine. If /mcp shows nothing here, run: bash scripts/setup.sh (or the 'claude mcp add' command in RUN.md).",
  "mcpServers": {
    "oncall": {
      "command": ".venv/bin/python",
      "args": ["oncall_server.py"]
    }
  }
}
